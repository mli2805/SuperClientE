<Window x:Class="SuperClientCa.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <GroupBox DockPanel.Dock="Left" Header="Servers" Width="200">
            <DockPanel Margin="5">
                <Button DockPanel.Dock="Bottom" Width="150" Height="25" Content="Add server" Margin="5" Name="AddServer"/>
                <Button Width="150" Height="25" Content="Connect server" Margin="5" Name="AddChild"/>
            </DockPanel>
        </GroupBox>
        <DockPanel DockPanel.Dock="Left" Background="Beige">

        </DockPanel>
        <TabControl ItemsSource="{Binding TabItems}" SelectedIndex="{Binding SelectedItemIndex}" Visibility="Visible">
          
        </TabControl>

    </DockPanel>

</Window>